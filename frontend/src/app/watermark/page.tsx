export default function WatermarkPage() {
    return (
        <div>
            <h1>FiduScan Watermark Integration</h1>
            <p>Upload Image, Audio, or Video to trigger the end-to-end embedding and verification workflow.</p>
        </div>
    )
}
