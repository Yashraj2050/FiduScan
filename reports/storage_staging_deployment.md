# Phase 5B — Storage Staging Deployment
*Generated: 2026-05-31 01:36 IST*

## Object Storage Provisioning
Google Cloud Storage (GCS) buckets have been successfully provisioned to handle user media uploads securely and asynchronously.

### Provisioned Buckets
1. **Quarantine Bucket:** `gs://fiduscan-staging-quarantine`
   - **Purpose:** Secure holding area for unverified images, audio, and video pending forensic analysis.
   - **Access:** Private. Backend Service Account only.

2. **Processed Bucket:** `gs://fiduscan-staging-processed`
   - **Purpose:** Storage for validated media and generated analysis heatmaps (Grad-CAM).
   - **Access:** Private. Accessed via Signed URLs generated by the Backend API.

## Storage Configurations

### 1. Encryption
- Enabled **Customer-Managed Encryption Keys (CMEK)** via Google Cloud KMS.
- All media assets are encrypted at rest before being written to disk.

### 2. Retention & Lifecycle Policies
- Configured a 24-hour Time-to-Live (TTL) lifecycle rule on the `quarantine` bucket to automatically delete orphaned uploads.
- Configured a 30-day archival tiering rule on the `processed` bucket for cost efficiency.

### 3. CORS
- Configured Cross-Origin Resource Sharing (CORS) on the quarantine bucket to accept direct uploads (Signed PUT requests) strictly from `https://staging.fiduscan.com`.

**Status: OBJECT STORAGE DEPLOYED AND SECURED**
